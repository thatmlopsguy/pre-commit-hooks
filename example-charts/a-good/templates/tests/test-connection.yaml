apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "good.fullname" . }}-test-connection"
  labels:
    {{- include "good.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "good.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
